package it.polimi.ingsw.model.player;

import it.polimi.ingsw.model.game.Game;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;

//TODO: everything

/*public class FaithTrackTest {
    private Game game;
    private FaithTrack ft;

    @Before
    public void setUp() {
        // game=new Game();
        ft = new FaithTrack();
        assertNotNull(game);
        assertNotNull(ft);
        assertEquals(0, ft.getPosition());
        assertEquals(2, ft.getFigures()[0]);
        assertEquals(3, ft.getFigures()[1]);
        assertEquals(4, ft.getFigures()[2]);
    }

    @Test
    /*public void advanceTest(){
        int s1=1, s2=2, s3=3, s4=0;
        int p;

       p= ft.advance(s1,game);
       assertEquals(1,ft.getPosition());


    }


    @After
    public void tearDown(){}





} */